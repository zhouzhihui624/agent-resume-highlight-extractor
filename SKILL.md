---
name: agent-resume-highlight-extractor
description: 当用户希望从仓库、文档、PRD、提交、测试、提示词、运行轨迹或项目笔记中提取和强化 AI Agent 或大模型工程经历，用于简历、实习申请、项目总结、STAR 故事、面试深挖或岗位定制时使用。尤其适用于提到简历亮点、项目包装、含金量、AI Harness、Agent evaluation、评测、底层原理、代码实现链路、开发细节、面试话术、resume highlights、个人贡献边界，或者要求生成能讲清初始状态、内容来源、角色分工、机制关系和完整流程的通俗深挖文档，以及已有材料内容分散、没有主线、看完记不住、术语太多、流程图重复、只讲结果不讲怎么实现、重构后细节丢失的场景。
---

# AI Agent 简历亮点提取器

## 目标

把真实项目能力转化为用户能够在面试中连续讲清、经得住追问并记得住的项目故事。先做技术尽职调查，再还原工程问题、选择适合实习生承担的有限职责、建立叙事主线，最后才压缩成简历和面试表达。目标不是生成章节齐全的技术百科，而是让每个亮点形成一个主角明确、因果完整、能够逐层展开的故事。

除非用户要求其他语言，否则默认使用中文。面向初学者时，必要术语第一次出现应写成 `中文解释（English Term）`，后续优先使用中文说法。源码标识、API、模型名和提供方名称在证据部分保持原样。

## 不可违反的原则

1. 先检查可用证据，再起草亮点。
2. 默认采用**实习包装模式**：仓库中有证据的项目能力可以进入候选工作池，再收敛成实习生可信的模块、链路、实验、接入或验证职责；不要因为缺少个人提交证明而停下来核对归属。
3. 包装必须建立在真实机制上。可以主动提炼合理的定性价值、上线用途和可衡量方向，但不得凭空编造精确数字、生产部署事件、客户规模或无法由机制推出的业务收益。
4. 如果缺少编排、工具、状态、评测或相当证据，不得把普通大模型接口接入包装成 Agent 平台。
5. 严格遵守用户要求的范围。用户只要两条简历内容时，内部完成必要分析后只返回两条。
6. 一个亮点只能有**一条核心主张和一条因果主线**。细节必须服务主线，不能把角色、状态、流程和故障并列成没有重点的知识清单。
7. 实习包装模式下可以直接使用 `参与建设`、`负责其中 X 环节`、`围绕 X 完成实现、评测、接入或验证` 等有限归属动词，不要求先证明个人提交；不得据此升级成 `主导整个平台`、`独立负责完整链路` 或明显超出实习周期的所有权。
8. 当任务同时包含简历包装和面试准备时，同一亮点的简历、30 秒、2 分钟、10 分钟和 STAR 必须来自同一条故事主线，不得分别生成互相漂移的版本。
9. 先教学，再命名。面对实习生、初学者或明确要求通俗解释的用户，不要以未解释术语、缩写堆叠、源码标识或架构标签开场。
10. 通俗表达必须保留技术深度。简化的是讲解顺序和词汇，不是状态流、决策逻辑、恢复行为、限制和取舍。
11. 对学生和实习生，应缩小**责任范围**，而不是降低技术深度。必须收敛为项目证据支持、工作量可信且本人能够讲透的模块、链路、实验、接入或验证职责，并说明周边团队边界。
12. 用户最终只保留一份主题文档时，采用**主线正文 + 技术深挖附录**的单文档整合模式。主线清晰不等于删除细节；支撑主流程的实现细节必须紧跟对应步骤，扩展矩阵和证据再放附录。有价值且已核验的旧内容必须迁移到稳定位置，不能把篇幅缩短当作重构成功标准。
13. 不得用架构名称、功能介绍、结果表现或价值判断代替实现说明。每个正式亮点必须从代码和测试中还原至少一条完整执行链，讲清对象、规则、状态、调用顺序、失败恢复和验证方式；不逐文件讲源码不等于不讲具体实现。
14. 正文遵循**流程内闭环**：先用一张总图展示方法，再按真实链路拆成 2-4 个自然阶段，通常优先使用三个。每个阶段先讲清“拿到什么、做了什么、得到什么”，再只补充理解该阶段所必需的规则、状态、失败和验证；不得为了模板完整把所有细节平均塞进每一步。
15. 默认只保留一张承担入口作用的整体流程图。只有并发、跨角色等待或严格时序本身就是核心难点时，才额外使用时序图；不得用两张图重复同一条主流程。
16. 通俗表达约束覆盖标题、流程图节点、表格、正文和面试话术，而不只是术语表。先说业务含义，再在必要时补充源码或英文名称；快速复习层不得出现未解释的源码状态名、缩写或协议标签。
17. 批量重构文档包时，先区分正式亮点、总览或索引、关系说明、独立项目、候选储备和历史材料。只有正式亮点套用完整深挖结构；校验应检查语义合同，而不是强迫所有文档使用同一组固定标题。
18. 流程图包含多种实验模式、运行路径或策略分支时，必须明确这些分支是**互斥选择、并行执行还是先后步骤**。互斥分支不得混用“步骤一、步骤二、步骤三”编号，也不得把只属于某一分支的处理画成所有分支都会经过的公共步骤。
19. `冻结、物化、基线、检查点、克隆、收敛、准入` 等抽象工程词不能只放进术语表。第一次出现时必须说明“具体保存或恢复了什么、读者能观察到什么、它影响哪一步判断”。例如解释基线时优先说明 `git status`、`git diff` 和最终计分分别会看到什么，而不是只给内部引用名称。
20. 长链路先给角色分工和一次从输入到结果的完整走读，再拆用户代理、Judge、恢复或报告等子机制。至少选择一个贯穿示例，展示输入何时可见、状态怎样变化、失败如何体现和结果如何聚合，避免读者从多个局部章节自行拼接主流程。
21. 正式亮点必须在第一次生成时说明**内容来源和生命周期**：开始前已经存在什么、本轮按需加载什么、模型生成或修改什么、最终保存或发布什么。不得让读者自己猜测“基础骨架、运行时指导和最终产物”之间的关系。
22. 多个机制共同解决一个问题时，不得并排写成功能清单。必须沿时间或因果关系说明前一个机制为什么不够、后一个机制接管哪个阶段、它们怎样共同形成完整能力。
23. 正式生成前必须完成讲解底稿和普通程序员可读性检查。不能清楚回答起点、角色、输入、阶段、机制关系和最终产物的主题，即使代码很多，也先继续调查或缩小范围，不直接生成长篇文档。
24. 用户反馈“太复杂、内容太多、没看懂”时，应回到首次出现的位置替换原有抽象表述并删除重复段落，不得只在文末追加解释。但这属于质量兜底，首要目标仍是第一次生成就清楚。

## 请求路由

调研前先选择一个主要交付模式：

| 用户请求特征 | 默认交付物 | 按需读取 |
| --- | --- | --- |
| 实习生、学生、项目包装、直接写成工作内容、不核对个人贡献 | 实习包装模式：从项目能力中选择可信的有限职责并直接生成第一人称材料 | 两份参考指南 |
| 明确要求逐项 ownership 审计、仅按提交或任务记录核验、不推断个人贡献 | 证据严格模式：区分个人锚点、协作范围和架构理解 | `references/evidence-and-theme-guide.md` |
| 允许包装指标或上线效果 | 在实习包装模式中补充机制可推出的定性价值、上线用途和指标方向；精确数字与实际生产上线仍需证据或用户提供 | `references/output-recipes.md` |
| 只要简历内容、严格数量或长度 | 严格按要求返回简历内容 | `references/output-recipes.md` |
| 旧文档或机制证据薄弱 | 保守草稿、证据缺口和最多 3 个关键问题 | `references/evidence-and-theme-guide.md` |
| 项目分析、包装、含金量、4-5 个亮点 | 按证据排序的完整分析，最终保留 2-5 个主题 | 两份参考指南 |
| 面试、STAR、底层原理、deep dive | 最强主题的面试深挖包 | 两份参考指南 |
| 太浅、不够详细、缺少开发细节、只讲结果不讲怎么实现、面试会被问倒 | 先建立代码实现事实卡，再生成包含对象、决策规则、状态变化、完整执行链、失败恢复、替代方案和验证方式的深挖文档 | 两份参考指南 |
| 内容分散、没有主线、看完记不住、缺少主体性 | 重建故事主张和连续回答链；实现细节跟随对应流程步骤，扩展材料再进入附录 | `references/output-recipes.md` |
| 最终只保留一份文档、担心重构后细节减少 | 单文档整合模式：快速复习层建立主线，按 2-4 个自然阶段就地讲清核心机制，附录保存扩展矩阵与证据 | `references/output-recipes.md` |
| 保存或重构文档包 | 一份总览和每个强主题各一篇文档 | 两份参考指南 |
| 同一目录混有总览、关系说明、候选和独立项目 | 先标注文档角色，只对正式亮点执行完整模板和亮点数量统计 | `references/output-recipes.md` |
| 看不懂、太多英文、很懵、循序渐进 | 先检查首次生成是否遗漏起点、内容来源、角色或机制关系，再按初学者教学顺序重写；替换原文而不是继续追加补丁 | `references/output-recipes.md` |
| 流程图看不懂、分支像顺序、实现细节只在后续追问出现、旧校验器要求固定标题 | 先标明分支是互斥、并行还是顺序，再改成一张总流程图和流程内闭环；必要时补“测试什么、候选做什么、输出什么”对照表 | `references/output-recipes.md` |
| 针对 JD 或岗位定制 | 岗位要求与证据映射，以及定制简历内容 | `references/output-recipes.md` |

用户明确指定的格式优先于此表。不要以“补充完整”为由附加用户没有要求的章节。

## 工作流程

### 1. 明确范围

确认以下信息：

- 可用材料和仓库根目录；
- 目标岗位和资历层级；
- 用户要求的数量、长度、语言和保存位置；
- 用户需要简历内容、项目理解、面试准备还是三者都要；
- 使用实习包装模式还是证据严格模式；
- 用户已经提供的个人贡献、指标和上线信息；
- 拟写范围对用户资历和实习时长是否可信。

用户自称实习生、学生，或明确要求“包装”“直接写成我的工作”时，默认进入实习包装模式，不因个人贡献证据缺失而追问。只有用户明确要求逐项核验个人归属、仅按提交或任务记录判断、真实性审计，或明确要求不要推断个人贡献时，才进入证据严格模式。普通的“不要夸大团队成果”“不要写成我主导”仍属于实习包装模式，只需缩小职责范围。

能够安全给出部分结果时，不要因为信息不全而停住。实习包装模式只在机制本身不清、精确数字无法确定或实际生产上线状态会改变表述时询问；个人归属本身不是阻塞项。

### 2. 检查证据

先阅读用于定位的材料，再用更强证据验证重要表述：

1. 项目说明、`README`、架构文档、PRD 和已有项目理解文档；
2. 源码入口、接口、提示词、数据结构、配置和迁移；
3. 测试、评测、固定样本、运行轨迹、回放产物、CI 和基准报告；
4. 提交历史、PR/MR、日志和用户明确陈述。

存在中文文档目录时应尽早检查，但在得到其他证据印证前，只把它作为定位材料。文档和实现不一致时，采用范围更窄且已确认的事实，并说明不确定性。

大型项目只有在支持委派且用户允许时才使用子 Agent。按相互独立的证据范围或互不重叠的目标文档拆分任务，让每个子 Agent 输出已确认事实、状态时序、反例、过度表述和未知项；不要让多个子 Agent 同时改同一文件。主题选择、术语翻译、职责范围、证据口径、文档链接和最终措辞必须由主 Agent 统一检查。

批量处理现有文档包时，先建立文档角色表：

| 角色 | 处理方式 |
| --- | --- |
| 正式亮点 | 参与亮点筛选，使用完整叙事与实现结构 |
| 总览或索引 | 只负责项目定位、阅读路径和简历汇总，不强行套固定阶段模板 |
| 关系说明 | 解释两个仓库、平台或评测层级的边界，不计为简历亮点 |
| 独立项目 | 单独形成项目故事，不塞进主项目凑亮点 |
| 候选储备或历史材料 | 保留作参考，但不进入正式推荐路径 |

项目包含多个 Agent 或大模型子系统、评测术语容易混淆，或者需要判断某项机制是否足以成为主亮点时，读取 `references/evidence-and-theme-guide.md`。

### 3. 从代码还原实现链路

在写故事主张、结果价值和面试话术之前，先为每个候选建立一张内部**实现事实卡**。不得只根据 README、模块名或架构图补全下面内容：

| 实现事实 | 必须从源码、配置、测试或轨迹回答的问题 |
| --- | --- |
| 触发与入口 | 谁在什么条件下调用该机制，正常入口和恢复入口是否相同？ |
| 初始状态与内容来源 | 开始前已经有哪些默认文件、配置或历史版本？本轮加载的是指导材料还是业务数据？哪些内容由模型新生成或修改？ |
| 输入与输出 | 接收哪些对象或参数，产生哪些结果、事件或中间制品？ |
| 核心对象 | 使用哪些数据结构、配置、目录、协议、索引或状态字段？ |
| 决策规则 | 按什么顺序、条件、算法、阈值、预算或优先级作出判断？ |
| 状态与副作用 | 哪些状态被创建、冻结、更新或持久化，何时执行真实副作用？ |
| 执行链 | 上一步怎样成为下一步输入，跨模块或跨仓库怎样衔接？ |
| 失败与恢复 | 参数错误、业务失败、基础设施错误、中断或重启后分别怎样处理？ |
| 验证方式 | 哪些测试断言、评测结果、日志或产物证明上述合同成立？ |

每个候选至少还原一条正常执行链和一条主要失败或恢复链。实现事实不足时标记为 `需补机制证据`，不得先用“提升效果”“实现闭环”或通用 Agent 知识填空。正文把代码翻译成对象关系、判断规则和运行流程；具体文件、函数和行号放入证据附录。

### 4. 还原工程问题并建立候选故事卡

不要从 `Runtime`、`Replay`、`HITL` 或模块目录名称直接生成亮点。先还原该机制为什么存在，为每个候选点建立一张内部故事卡：

| 故事卡字段 | 必须回答的问题 |
| --- | --- |
| 场景与问题 | 谁在什么流程中遇到了什么具体问题？ |
| 简单方案 | 最直接的实现是什么，为什么在这里不够？ |
| 反事实 | 如果删除这项机制，系统会退化或失败成什么样？ |
| 关键机制 | 哪些数据、状态或控制决策真正解决了问题？ |
| 机制关系 | 多个机制是顺序接力、互斥选择还是并行协作？前一个机制为什么不足，后一个机制接管什么？ |
| 可验证结果 | 形成了什么可以由测试、产物、运行记录或用户陈述支持的能力？ |
| 证据与边界 | 哪些材料支持上述每一部分，哪个更强说法仍不成立？ |

回答不出工程问题、简单方案为何不足、内容从哪里来、机制怎样衔接或反事实结果的候选点，通常只是功能清单，不应成为主亮点。

### 5. 选择包装策略并确定实习职责锚点

先选择一种模式，不要在同一份成品里来回切换。

#### 实习包装模式，默认

把有项目证据的能力视为候选工作池，从中推导一个**实习生可信的有限职责锚点**，直接生成第一人称简历和面试材料，不要求提交、任务记录或用户再次确认归属。职责锚点必须同时满足：

1. 只覆盖一个明确模块、链路、实验、接入或验证环节；
2. 能归纳为 1-3 个具体技术动作，而不是“参与整个平台”；
3. 工作量在常见实习周期内可信；
4. 能沿项目机制讲清一次流程、一个失败和一个取舍；
5. 能指出上游输入和下游团队边界。

推荐使用 `参与建设`、`负责其中 X 环节`、`围绕 X 完成实现/评测/接入/验证`。如果候选主题只能通过“主导完整平台”才能成立，继续缩小范围；仍无法缩小时淘汰，不向用户追问所有权。

#### 证据严格模式，仅显式触发

仅当用户明确要求逐项 ownership 审计、仅按提交或任务记录核验、真实性核验，或明确要求不要推断贡献时，才把候选拆成三层。普通的“区分团队边界”不触发严格模式：

| 层级 | 定义 | 用法 |
| --- | --- | --- |
| 个人锚点 | 用户明确实现、设计、调试、评测或沉淀的有限对象 | 可以成为简历和 STAR 的主语 |
| 协作范围 | 用户参与联调、方案讨论、接入或验证，但不拥有完整模块 | 作为协作背景，必须说明上下游边界 |
| 架构理解 | 仓库存在但没有个人参与证据 | 只用于项目理解和追问准备，不写成个人贡献 |

严格模式缺少个人锚点时，最多询问三个单一决策问题，并可生成项目理解稿。不要把严格模式的归属门槛反向施加给实习包装模式。

对表述可用性进行分类：

- `可直接包装`：工程问题和机制成立，且能收敛出可信的实习职责锚点；
- `可直接写`：严格模式下工程问题、机制和个人锚点都有支持；
- `需补机制证据`：无法解释真实流程、决策或结果，不能靠通用 Agent 知识补齐；
- `需补精确结果`：可以先写定性价值，但精确数字或实际生产上线仍需材料；
- `不使用`：缺少因果闭环、工作量明显失真，或只是相邻平台能力。

除非用户要求证据审计，否则不必输出完整故事卡和三层地图。

### 6. 选择真正有含金量的主题

选择 **2-5 个相互独立的主题**，不要把数量当作必须填满的配额。每个入选主题先通过四个门槛：

1. 有明确工程问题和可验证机制；
2. 实习包装模式下能收敛出可信的有限职责锚点；证据严格模式下有个人锚点或明确标记待确认；
3. 能形成从问题到结果的完整因果故事；
4. 能让没看过源码的普通程序员沿“起点 -> 输入 -> 关键阶段 -> 结果”复述，而不是只能罗列模块名和术语。

再按以下维度排序：证据强度、问题重要性、机制区分度、故事可讲解性、职责范围可信度、与目标岗位的相关性、实习工作量可信度，以及能否支撑连续追问。

对候选主题做两两去重。如果两个主题在工程问题、核心机制、主要证据和工程价值中有两项以上相同，优先合并成一个主故事，把另一个降为子机制或追问分支。

### 7. 先写一句故事主张

正式生成文档前，必须为每个主题写出一句内部主张：

```text
在[具体场景和问题]下，用户负责或参与[实习生可信的有限对象]，
通过[不超过三个关键设计]，形成[可验证结果]，但不扩大为[事实边界]。
```

实习包装模式允许根据项目能力推导有限职责锚点并使用第一人称。证据严格模式中个人贡献未知时，把主语改成 `项目` 或 `该模块`，并明确这是项目理解稿。故事主张写不清，说明主题范围过大或机制证据不足，应继续拆分或淘汰。

### 8. 生成前建立讲解底稿

故事主张通过后，先在内部完成下面的讲解底稿，再写流程图和正文。讲解底稿不是额外交付章节，而是保证第一次生成就清楚的写作输入：

| 必须回答 | 生成正文时怎样使用 |
| --- | --- |
| 改进前系统怎样工作，具体哪里会失败 | 形成开头的问题和最简单方案为什么不够 |
| 本次从什么状态开始 | 说明空白骨架、已有版本、历史任务或当前会话等真实起点 |
| 哪些内容原本存在、按需加载、由模型生成、最终保存 | 解释内容来源，避免把指导材料和最终产物混在一起 |
| 哪些角色参与，各自只负责什么 | 形成精简角色说明，不让读者混淆 Builder、目标 Agent、候选模型、用户代理或 Judge |
| 流程可以自然分成哪 2-4 个阶段 | 决定总图和正文顺序，不为凑模板强拆三步 |
| 多个机制怎样接力、互斥或并行 | 形成因果链，说明前一个机制为什么还不够 |
| 最终生成什么制品、状态或判断，怎样验证 | 形成结果、失败终态和事实边界 |

底稿必须能压缩成下面这条通俗链路：

```text
原来怎样以及哪里出问题
-> 从什么起点开始
-> 谁拿到什么输入
-> 依次做哪几件事
-> 最终保存、判断或发布什么
```

这条链路讲不顺时，先继续调查、合并机制或缩小亮点范围，不得靠增加术语、表格和章节掩盖主线缺失。

### 9. 建立一条面试叙事主线

正文和面试回答统一按以下因果顺序展开：

1. 发生了什么问题；
2. 最简单方案为什么不够；
3. 用户的任务和责任边界是什么；
4. 用户作出了哪一到三个关键设计或技术动作；
5. 一次任务怎样沿数据、状态和控制流完整运行；
6. 主要失败在哪里，怎样停止、恢复或暴露；
7. 最终形成什么可验证能力；
8. 仍有什么限制，哪个更强说法不能成立。

主线最多保留三个设计锚点、两个主要失败场景和一个核心取舍。每个阶段先说明输入、动作和输出，再补充让该阶段成立所必需的对象、规则、状态、失败处理和验证；只有角色全集、完整状态矩阵、规则全集、源码路径和低频边缘问题进入追问附录。一个事实只在主线中完整解释一次，后续版本通过压缩或展开复用，不得换一套逻辑重复介绍。

### 10. 解释机制并按需展开

技术深度取决于是否讲清具体实现、决策和边界，不取决于篇幅。内部实现事实卡可以完整记录触发、对象、状态、失败、验证和取舍，但正文不必把这些字段平均展开。每个自然阶段首先只回答：

```text
这一步拿到什么
-> 做了什么关键动作或判断
-> 产生什么结果并交给下一步
```

如果省略某个状态、规则、失败或验证会导致读者误解主流程，再把它紧跟在对应阶段补充；完整状态矩阵、低频异常和源码证据放入附录。不得为了显得深入而让每一步机械重复“为什么、对象、状态、失败、验证、取舍”七项内容。

算法或检索主题应说明输入字段、排序或判断方式、阈值与预算、稳定性和降级；状态型主题应说明状态字段、转换、持久化和 Resume；协议型主题应说明正向请求转换、反向结果解析和真实执行边界；评测型主题应说明 Case、Run、Judge、聚合和报告怎样衔接。只写“使用某算法”“建立某闭环”“支持某能力”不算实现说明。

面向初学者时，先用日常问题或最小场景建立直觉，再走完正常流程，然后围绕三个设计锚点解释机制、失败和取舍。术语第一次出现时写成 `中文解释（English Term）`，源码标识主要放在证据附录。图示之后必须用自然语言沿同一主线走一遍。

以下三类关系必须在第一次生成时主动讲清，而不是等待用户追问：

```text
内容来源：开始前已有内容 -> 本轮按需加载的指导 -> 模型生成或修改 -> 最终保存的产物
机制接力：什么时候出问题 -> 前一个机制为什么不够 -> 后一个机制接管什么 -> 怎样形成闭环
状态生命周期：当前状态是什么意思 -> 允许什么操作 -> 什么条件进入下一状态 -> 失败停在哪里
```

例如，生成型系统不能只写“读取定义并按需加载规范”，还要区分基础骨架、固定系统规则、运行时加载的指导材料和最终生成的业务资产。并发或恢复系统不能只列“队列、锁、Turn、WIP”，而要说明这些机制分别覆盖哪个时间段以及为什么需要接力。

对多分支流程图，绘图前先为每条边标注语义：

```text
顺序：上一步完成后进入下一步
互斥：本次只选择其中一条路径
并行：多条路径同时执行，汇合前必须全部完成或满足明确条件
```

互斥实验模式优先画成：

```text
准备共同起点
-> 选择要回答的问题
-> 三条独立实验路径
-> 只在报告层汇合
```

图后立即补一张 `测试什么 / 候选实际做什么 / 最终产出什么` 对照表。不得让读者从菱形、箭头位置或“步骤二只出现在一条分支”中猜测流程语义。

抽象术语采用**可观察行为改写法**。正文第一次出现时按下面顺序解释：

```text
这个词在这里指什么具体对象
-> 系统实际执行了什么动作
-> 用户、候选或 Judge 能看到什么
-> 如果省略这一步会出现什么错误
-> 最后再补源码名称
```

例如不要只写“物化冻结材料”，应写成“在全新沙箱中恢复审核时确定的仓库、起始提交、代码修改和任务文件”；不要只写“三个 Git 基线”，应说明历史修改在候选的 `git diff` 中是否可见，以及最终评分从哪个起点扣除已有修改。

当完整能力由多个角色共同完成时，先列出角色职责，再用编号走完一次 `准备任务 -> 候选运行 -> 受控交互 -> 独立验收 -> 结果聚合`。随后各小节只深挖其中一个环节。贯穿示例应复用同一任务，避免每个章节重新发明不同例子。

正式长文档默认采用以下阅读顺序：

```text
半页内的快速复习：问题、有限职责、核心方法和结果
-> 一张整体方法流程图
-> 用自然语言把图从头走一遍
-> 按图中顺序讲 2-4 个自然阶段
-> 集中说明主要失败、核心取舍和验证结果
-> 给出简历与用户要求的面试话术
-> 最后放高频追问和证据附录
```

整体图默认使用业务动作和中文判断条件，不直接放结构体名、状态常量、协议名或缩写。自然阶段必须和图中的真实执行顺序对应，通常使用三个，但不得为了凑数拆分或合并。不能先写一遍“流程”，再用“代码怎样实现”和“关键设计”重复第二、第三遍。高频追问可以换成面试问法复述已讲机制，也可以继续展开低频边界，但不能首次引入决定主流程成立的对象、规则或恢复行为。

多种面试版本必须同源：

- 30 秒只保留问题、实习职责锚点、关键机制和结果；
- 2 分钟沿完整主线各讲一句，并展开一个失败场景；
- 10 分钟沿同一顺序增加状态、替代方案和边界，必须形成连续讲述，不能只给时间表；
- STAR 复用同一事实，其中行动部分必须是职责锚点内、能够由项目机制解释的具体技术动作。

只生成用户明确要求的版本。用户只要 10 分钟讲法时，不要自动附加术语表、30 秒、2 分钟和完整 STAR。

#### 单文档整合模式

用户明确最终只保留一份主题文档时，重构前先建立旧内容迁移清单，把每项已核验细节标记为 `进入主线`、`移入深挖附录` 或 `因重复、错误、无关而删除`。不得以行数、标题数或压缩比例作为成功指标。

正文仍只突出不超过三个设计锚点、两个主要失败和一个核心取舍，并沿 2-4 个自然阶段解释主流程成立所必需的对象关系、决策规则、状态变化和恢复行为；附录保存完整矩阵、低频异常、替代方案扩展、观测成本、指标口径、事实边界和源码证据。用户要求“一个文档讲透”时，附录可以超过默认 3-5 个深挖点，但每项必须能回到同一主线，不能重新变成重复百科。

### 11. 执行事实边界审计

起草最终表述前，按以下维度检查每个重要事实：

| 检查维度 | 必须区分 |
| --- | --- |
| 时间范围 | 正常连续流程与崩溃或重启恢复 |
| 进程范围 | 同一次调用或同一进程，与跨进程或跨实例 |
| 持久化 | 内存状态与耐久共享状态 |
| 接入范围 | 内核能力与已经接入的具体协议或适配器 |
| 原子性 | 关联编号与去重、事务或严格一次保证 |
| 约束强度 | 提示词要求或任务范围，与工具权限、文件隔离或其他强制执行 |
| 终态 | 业务回合完成与构建、评测或部署成功 |
| 制品状态 | 草稿、候选或结果文件，与已验证版本、强制门禁或已发布制品 |
| 当前事实 | 已实现行为与改进建议或理想设计 |

证据只支持范围更窄的一侧时，必须使用更窄的说法。例如：评审建议不等于强制门禁；阶段产物不等于成熟评测平台；稳定交互编号不等于端到端幂等；业务回合成功结束不一定代表构建成功。

### 12. 保守起草

使用 `有限实习职责 + 运行机制 + 解决的问题或价值 + 结果`。

实习包装模式下，不做个人贡献审计，直接从项目能力中选取有限职责并使用 `参与建设`、`负责其中 X 环节`、`围绕 X 完成实现/评测/接入/验证`。只有用户明确要求证据严格模式时，才要求这些归属动词得到用户陈述、任务记录、提交或其他证据支持。两种模式都不得用柔和动词掩盖平台级所有权；可信亮点必须指出有限对象、具体技术动作和至少一个上下游团队边界。

结果包装按强度分三层：

1. `已验证结果`：测试、报告、日志、提交或用户信息直接支持，可以陈述具体能力、指标和上线状态；
2. `机制可推出的价值`：没有量化数据，但机制与结果之间存在清晰因果关系，可以写 `降低回归风险`、`提升可复现性`、`缩短定位路径`、`支撑上线前验收` 等定性价值；
3. `待核验的精确结果`：具体百分比、时长、用户量、成本金额、生产流量和“已上线”事件缺少证据时，只能作为建议补充的指标或话术备选，不得伪装成已发生事实。

用户明确说“指标和上线效果可以包装”时，应主动选择与机制匹配、面试价值更高的定性结果和上线用途，不要退化成只有 `支持`、`完成` 的流水账；但这句话本身不构成某个具体数字或生产部署已经发生的证据。

默认每个强主题都同时给出：`机制可推出的定性价值`、`非事实式上线用途` 和 `可量化指标方向`。没有实际生产证据时，上线用途写成 `支撑上线前`、`可用于发布前` 或 `为上线验收提供`，不能写成已经部署。

没有指标时，优先写机制能够直接带来的定性价值，并把可采集的量化口径放在附录；不要在可直接使用的简历内容中插入占位数字。

已有测试只能证明某项行为被覆盖，不能证明当前所有测试通过。只有本次任务实际运行并观察到成功时，才能声称测试通过。

生成结构化简历内容、STAR 故事、岗位定制版本或保存文档包前，读取 `references/output-recipes.md`。

## 面试主题文档要求

保存或重写长篇亮点文档时，正文必须是一条可以连续复述的主线，而不是把所有知识点平铺成百科。正文至少包含：半页内的快速复习、工程问题、个人任务与团队边界、一张整体方法图、图后自然语言讲解、按同一顺序展开的 2-4 个自然阶段、主要失败与取舍、结果与事实边界，以及用户明确要求的面试讲法。

以下内容按需放入追问附录，不得默认挤入正文：完整术语表、所有角色和制品、完整状态矩阵、主流程之外的规则全集、边缘失败、源码路径、证据清单和大规模追问题库。默认只保留 3-5 个最能解释主线的深挖点，除非用户明确要求完整题库。

用户明确最终只保留一份文档并要求深入时，还应保留能够支撑三层以上追问的已核验机制细节。此时“3-5 个深挖点”只是正文外的默认精简策略，不是删除完整控制链、状态边界或失败语义的硬上限。

机制证据薄弱时，不要用通用 Agent 知识虚构流程、失败经历或技术取舍。实习包装模式可以推导有限职责，但不能推导仓库中不存在的机制；证据严格模式才在个人锚点缺失时改为项目理解稿并询问最多三个单一决策问题。

## 最终检查

回答或保存文件前确认：

- 输出严格符合用户要求的范围和数量；
- 开头存在一句能够独立复述的故事主张；
- 开头的快速复习能够在半页内回答问题、有限职责、核心方法和结果；
- 正文只有一条从问题、个人任务、关键设计、完整流程、失败取舍到结果边界的因果主线；
- 正文已经说明开始前存在什么、本轮加载什么、模型生成或修改什么、最终保存或发布什么；
- 多个机制已经按顺序接力、互斥选择或并行协作解释，没有并排写成缺少因果关系的功能表；
- 每个核心机制都有证据，结果属于已验证结果或机制可推出的定性价值；
- 实习包装模式已经直接生成可信的有限职责，没有停在个人归属确认；证据严格模式没有擅自推断贡献；
- 学生或实习生亮点收敛到可信的模块、链路、实验、接入或验证职责，没有用“参与”掩盖平台级所有权；
- 入选主题相互独立，并且足以支撑面试；
- 没有把待核验的精确指标、规模、生产部署事件或业务收益写成已发生事实；同时没有漏掉机制能够直接推出的定性价值和上线用途；
- 主线只突出不超过三个设计锚点，主流程必需细节已经就地解释，扩展矩阵和低频材料进入追问附录；
- 正文默认只有一张承担入口作用的整体流程图；额外图示确有独立信息价值，没有重复同一路径；
- 图中的节点和判断条件使用普通程序员能直接理解的中文，没有先抛源码状态名或英文协议；
- 多分支流程图已经明确标注互斥、并行或顺序关系，没有用公共步骤编号误导分支语义；
- 每种实验或运行模式都能用“测试什么、候选做什么、产出什么”三句话区分；
- 2-4 个自然阶段与整体图对应，每个阶段先讲清输入、关键动作和输出，只展开理解主流程必需的规则、状态、失败和验证；
- 核心实现没有被推迟到高频追问；删除追问区后，读者仍能理解主流程为什么成立；
- 单文档整合任务已完成旧内容迁移检查，每项高价值事实都有稳定位置或明确删除原因；
- 没有把行数减少、标题减少或篇幅压缩当作质量目标；
- 用户要求一个文档讲透时，附录仍足以回答对象、决策、状态、异常、取舍、观测和指标等递进追问；
- 每个正式亮点都从代码或测试还原了至少一条正常执行链和一条失败或恢复链；
- 每个设计锚点都说明了让该设计成立的关键对象、规则或算法，没有只写架构名称与结果，也没有机械重复全套模板；
- 整体流程图与正文描述的是同一条真实执行路径，并且图后能够用自然语言完整走一遍；
- 正文没有变成逐文件、逐函数源码导览，但证据附录能够把关键结论定位到代码、测试、轨迹或报告；
- 没有用性能结果、业务价值、上线用途或“形成闭环”等总结替代“代码具体如何实现”的说明；
- 同一事实没有在流程、原理、话术和 STAR 中用不同结构反复介绍；
- 30 秒、2 分钟、10 分钟和 STAR 只在用户需要时生成，并且沿用相同关键词、事实和顺序；
- 10 分钟版本是一段可连续讲述的回答，不只是章节目录或时间安排表；
- 没有把正常路径扩大成崩溃恢复，把同进程扩大成分布式恢复，或把内核能力扩大成所有协议均已接入；
- 没有把建议写成强制执行，把候选制品写成已验证或已发布结果，或把关联编号写成严格一次保证；
- 初学者文档遵循“问题、直觉、完整流程、机制、失败与取舍、面试表达”的顺序；
- 必要术语第一次出现时已解释，标题、流程图、表格和快速复习材料没有堆叠未解释英文标签，源码标识主要位于证据部分；
- 术语定义没有引入第二个未解释缩写或架构标签；
- `冻结、物化、基线、检查点、克隆、收敛、准入` 等词已经落到具体对象、动作和可观察行为，不是只在术语表中换一个近义词；
- 图示使用适合读者的标签，并在图后用自然语言走一遍；
- 多角色长链路在拆分子机制前已经给出角色表和一次完整走读，并至少有一个贯穿示例；
- 没有把模拟模型的运行时测试写成真实模型质量评测；
- 实习包装模式不会为了确认个人归属而提问；机制、精确数字或实际上线状态确实影响表述时最多询问三个高价值问题；
- 用户脱离文档后能够复述问题、个人职责、完整流程、一个失败、一个取舍和结果边界；
- 没看过源码的普通程序员能够说清流程起点、角色分工、机制关系和最终产物，不需要先阅读追问区；
- 文档包中的正式亮点、总览、关系说明、独立项目和候选材料已分类，非亮点文档没有被强行套用亮点模板或计入亮点数量；
- 文档校验检查的是流程、实现、失败、验证、面试和证据等语义合同，不依赖一组不可更改的章节标题；
- 保存的文档包不存在 `TODO`、`TBD`、过期重复主题或失效证据路径。
