---
name: agent-resume-highlight-extractor
description: 当用户希望从仓库、文档、PRD、提交、测试、提示词、运行轨迹或项目笔记中提取和强化 AI Agent 或大模型工程经历，用于简历、实习申请、项目总结、STAR 故事、面试深挖或岗位定制时使用。尤其适用于提到简历亮点、项目包装、含金量、AI Harness、Agent evaluation、评测、底层原理、面试话术、resume highlights、个人贡献边界，或者已有亮点文档内容分散、没有主线、看完记不住、缺少主体性的请求。
---

# AI Agent 简历亮点提取器

## 目标

把真实项目证据转化为用户能够在面试中连续讲清、经得住追问并记得住的项目故事。先做技术尽职调查，再还原工程问题、确认个人贡献、建立叙事主线，最后才压缩成简历和面试表达。目标不是生成章节齐全的技术百科，而是让每个亮点形成一个主角明确、因果完整、能够逐层展开的故事。

除非用户要求其他语言，否则默认使用中文。面向初学者时，必要术语第一次出现应写成 `中文解释（English Term）`，后续优先使用中文说法。源码标识、API、模型名和提供方名称在证据部分保持原样。

## 不可违反的原则

1. 先检查可用证据，再起草亮点。
2. 区分**项目具备的能力**和**用户个人贡献**。仓库里存在某项能力，只能证明前者。
3. 不得虚构所有权、实现深度、指标、规模、上线状态或业务影响。
4. 如果缺少编排、工具、状态、评测或相当证据，不得把普通大模型接口接入包装成 Agent 平台。
5. 严格遵守用户要求的范围。用户只要两条简历内容时，内部完成必要分析后只返回两条。
6. 一个亮点只能有**一条核心主张和一条因果主线**。细节必须服务主线，不能把角色、状态、流程和故障并列成没有重点的知识清单。
7. 个人贡献没有用户陈述、任务记录、提交或其他证据时，不得自动使用 `参与`、`负责`、`设计并实现` 等归属动词。此时只能输出项目能力理解稿、标记为待确认，或询问一个真正决定表述的问题。
8. 当任务同时包含简历包装和面试准备时，同一亮点的简历、30 秒、2 分钟、10 分钟和 STAR 必须来自同一条故事主线，不得分别生成互相漂移的版本。
9. 先教学，再命名。面对实习生、初学者或明确要求通俗解释的用户，不要以未解释术语、缩写堆叠、源码标识或架构标签开场。
10. 通俗表达必须保留技术深度。简化的是讲解顺序和词汇，不是状态流、决策逻辑、恢复行为、限制和取舍。
11. 对学生和实习生，应缩小**责任范围**，而不是降低技术深度。必须收敛为有证据支持的模块、链路、实验、接入或验证职责，并说明周边团队边界。

## 请求路由

调研前先选择一个主要交付模式：

| 用户请求特征 | 默认交付物 | 按需读取 |
| --- | --- | --- |
| 只要简历内容、严格数量或长度 | 严格按要求返回简历内容 | `references/output-recipes.md` |
| 旧文档、个人贡献不清或证据薄弱 | 保守草稿、证据缺口和最多 3 个关键问题 | `references/evidence-and-theme-guide.md` |
| 项目分析、包装、含金量、4-5 个亮点 | 按证据排序的完整分析，最终保留 2-5 个主题 | 两份参考指南 |
| 面试、STAR、底层原理、deep dive | 最强主题的面试深挖包 | 两份参考指南 |
| 太浅、不够详细、缺少开发细节、面试会被问倒 | 包含状态分层、失败窗口、替代方案和事实边界的深挖文档 | 两份参考指南 |
| 内容分散、没有主线、看完记不住、缺少主体性 | 重建故事主张和连续回答链；正文只保留主线，细节移入追问附录 | `references/output-recipes.md` |
| 保存或重构文档包 | 一份总览和每个强主题各一篇文档 | 两份参考指南 |
| 看不懂、太多英文、很懵、循序渐进 | 按初学者教学顺序重写，同时保留证据和深度 | `references/output-recipes.md` |
| 针对 JD 或岗位定制 | 岗位要求与证据映射，以及定制简历内容 | `references/output-recipes.md` |

用户明确指定的格式优先于此表。不要以“补充完整”为由附加用户没有要求的章节。

## 工作流程

### 1. 明确范围

确认以下信息：

- 可用材料和仓库根目录；
- 目标岗位和资历层级；
- 用户要求的数量、长度、语言和保存位置；
- 用户需要简历内容、项目理解、面试准备还是三者都要；
- 用户已经提供的个人贡献信息；
- 拟写范围对用户资历和实习时长是否可信。

能够安全给出部分结果时，不要因为信息不全而停住。如果缺少关键个人贡献或结果信息，先保守起草，只询问真正会改变表述的关键问题。

### 2. 检查证据

先阅读用于定位的材料，再用更强证据验证重要表述：

1. 项目说明、`README`、架构文档、PRD 和已有项目理解文档；
2. 源码入口、接口、提示词、数据结构、配置和迁移；
3. 测试、评测、固定样本、运行轨迹、回放产物、CI 和基准报告；
4. 提交历史、PR/MR、日志和用户明确陈述。

存在中文文档目录时应尽早检查，但在得到其他证据印证前，只把它作为定位材料。文档和实现不一致时，采用范围更窄且已确认的事实，并说明不确定性。

大型项目只有在支持委派且用户允许时才使用子 Agent。按相互独立的证据范围拆分任务，让每个子 Agent 输出已确认事实、状态时序、反例、过度表述和未知项；主题选择与最终措辞必须由主 Agent 统一完成。

项目包含多个 Agent 或大模型子系统、评测术语容易混淆，或者需要判断某项机制是否足以成为主亮点时，读取 `references/evidence-and-theme-guide.md`。

### 3. 还原工程问题并建立候选故事卡

不要从 `Runtime`、`Replay`、`HITL` 或模块目录名称直接生成亮点。先还原该机制为什么存在，为每个候选点建立一张内部故事卡：

| 故事卡字段 | 必须回答的问题 |
| --- | --- |
| 场景与问题 | 谁在什么流程中遇到了什么具体问题？ |
| 简单方案 | 最直接的实现是什么，为什么在这里不够？ |
| 反事实 | 如果删除这项机制，系统会退化或失败成什么样？ |
| 关键机制 | 哪些数据、状态或控制决策真正解决了问题？ |
| 可验证结果 | 形成了什么可以由测试、产物、运行记录或用户陈述支持的能力？ |
| 证据与边界 | 哪些材料支持上述每一部分，哪个更强说法仍不成立？ |

回答不出工程问题、简单方案为何不足或反事实结果的候选点，通常只是功能清单，不应成为主亮点。

### 4. 确认个人贡献并建立三层地图

把每个候选点拆成三层，不能混写：

| 层级 | 定义 | 用法 |
| --- | --- | --- |
| 个人锚点 | 用户明确实现、设计、调试、评测或沉淀的有限对象 | 可以成为简历和 STAR 的主语 |
| 协作范围 | 用户参与联调、方案讨论、接入或验证，但不拥有完整模块 | 作为协作背景，必须说明上下游边界 |
| 架构理解 | 仓库存在但没有个人参与证据 | 只用于项目理解和追问准备，不写成个人贡献 |

缺少个人锚点时，最多询问三个**单一决策问题**：具体负责哪个对象、本人做过哪些技术动作、有什么可核验结果或失败经历。一个编号中不要塞入多个宽泛子问题。

当用户要的是个人简历、STAR 或面试稿而个人锚点缺失时，先给出候选项目故事和上述确认问题，不要直接完成第一人称文档。用户明确只要项目机制理解，或者要求在信息不足时继续时，可以生成 `项目理解版`；其中面试讲法以 `项目` 或 `该模块` 为主语，把待确认项放在讲法之外，不要让候选人在面试开场先念证据免责声明。

对表述可用性进行分类：

- `可直接写`：工程问题、机制和个人锚点都有支持；
- `项目理解稿`：项目机制有支持，但个人贡献未知，只能帮助理解；
- `需本人确认`：补充一个具体事实后可能成为个人亮点；
- `不使用`：缺少因果闭环、依赖猜测，或只是相邻平台能力。

除非用户要求证据审计，否则不必输出完整故事卡和三层地图。

### 5. 选择真正有含金量的主题

选择 **2-5 个相互独立的主题**，不要把数量当作必须填满的配额。每个入选主题先通过三个门槛：

1. 有明确工程问题和可验证机制；
2. 有个人锚点，或者明确标记为待确认而不直接写进简历；
3. 能形成从问题到结果的完整因果故事。

再按以下维度排序：证据强度、问题重要性、机制区分度、个人贡献可信度、与目标岗位的相关性、实习工作量可信度，以及能否支撑连续追问。

对候选主题做两两去重。如果两个主题在工程问题、核心机制、主要证据和工程价值中有两项以上相同，优先合并成一个主故事，把另一个降为子机制或追问分支。

### 6. 先写一句故事主张

正式生成文档前，必须为每个主题写出一句内部主张：

```text
在[具体场景和问题]下，用户负责或参与[有证据的有限对象]，
通过[不超过三个关键设计]，形成[可验证结果]，但不扩大为[事实边界]。
```

个人贡献未知时，把主语改成 `项目` 或 `该模块`，并明确这是项目理解稿，不能生成第一人称 STAR。故事主张写不清，说明主题范围过大或证据不足，应继续拆分或淘汰。

### 7. 建立一条面试叙事主线

正文和面试回答统一按以下因果顺序展开：

1. 发生了什么问题；
2. 最简单方案为什么不够；
3. 用户的任务和责任边界是什么；
4. 用户作出了哪三个关键设计或技术动作；
5. 一次任务怎样沿数据、状态和控制流完整运行；
6. 主要失败在哪里，怎样停止、恢复或暴露；
7. 最终形成什么可验证能力；
8. 仍有什么限制，哪个更强说法不能成立。

主线最多保留三个设计锚点、两个主要失败场景和一个核心取舍。角色全集、完整状态表、全部规则、源码路径和边缘问题进入追问附录。一个事实只在主线中完整解释一次，后续版本通过压缩或展开复用，不得换一套逻辑重复介绍。

### 8. 解释机制并按需展开

技术深度取决于是否讲清决策和边界，不取决于篇幅。完整分析至少说明触发与输入、决策路径、关键状态变化、输出或判断、主要失败恢复，以及为什么没有采用更简单方案。当一个名称掩盖不同暂停点、持久化、副作用或终态时，先拆开比较再解释。

面向初学者时，先用日常问题或最小场景建立直觉，再走完正常流程，然后围绕三个设计锚点解释机制、失败和取舍。术语第一次出现时写成 `中文解释（English Term）`，源码标识主要放在证据附录。图示之后必须用自然语言沿同一主线走一遍。

多种面试版本必须同源：

- 30 秒只保留问题、个人锚点、关键机制和结果；
- 2 分钟沿完整主线各讲一句，并展开一个失败场景；
- 10 分钟沿同一顺序增加状态、替代方案和边界，必须形成连续讲述，不能只给时间表；
- STAR 复用同一事实，其中行动部分必须是有证据的个人技术动作。

只生成用户明确要求的版本。用户只要 10 分钟讲法时，不要自动附加术语表、30 秒、2 分钟和完整 STAR。

### 9. 执行事实边界审计

起草最终表述前，按以下维度检查每个重要事实：

| 检查维度 | 必须区分 |
| --- | --- |
| 时间范围 | 正常连续流程与崩溃或重启恢复 |
| 进程范围 | 同一次调用或同一进程，与跨进程或跨实例 |
| 持久化 | 内存状态与耐久共享状态 |
| 接入范围 | 内核能力与已经接入的具体协议或适配器 |
| 原子性 | 关联编号与去重、事务或严格一次保证 |
| 约束强度 | 提示词要求或任务范围，与工具权限、文件隔离或其他强制执行 |
| 终态 | 业务回合完成与构建、评测或部署成功 |
| 制品状态 | 草稿、候选或结果文件，与已验证版本、强制门禁或已发布制品 |
| 当前事实 | 已实现行为与改进建议或理想设计 |

证据只支持范围更窄的一侧时，必须使用更窄的说法。例如：评审建议不等于强制门禁；阶段产物不等于成熟评测平台；稳定交互编号不等于端到端幂等；业务回合成功结束不一定代表构建成功。

### 10. 保守起草

使用 `个人贡献 + 运行机制 + 解决的问题或价值 + 有证据的结果`。

只有个人锚点得到用户陈述、任务记录、提交或其他证据支持时，才使用 `参与建设`、`参与设计`、`负责其中 X 模块`、`设计并实现` 等归属动词。没有个人证据时，不得为了满足简历格式而推断用户参与关系；应输出项目能力理解稿、标记待确认，或者先询问一个关键问题。可信的实习生亮点必须指出有限对象、个人技术动作和至少一个上下游团队边界。

没有指标时：

- 描述 `支持`、`打通`、`沉淀`、`可复现`、`可回归` 或 `可恢复` 等已形成能力；
- 把可能补充的指标单独放在 `待补充指标`；
- 不要在可直接使用的简历内容中插入占位数字。

已有测试只能证明某项行为被覆盖，不能证明当前所有测试通过。只有本次任务实际运行并观察到成功时，才能声称测试通过。

生成结构化简历内容、STAR 故事、岗位定制版本或保存文档包前，读取 `references/output-recipes.md`。

## 面试主题文档要求

保存或重写长篇亮点文档时，正文必须是一条可以连续复述的主线，而不是把所有知识点平铺成百科。正文至少包含：一句故事主张、工程问题、个人任务与团队边界、一次完整流程、三个设计锚点、主要失败与取舍、结果与事实边界，以及用户明确要求的面试讲法。

以下内容按需放入追问附录，不得默认挤入正文：完整术语表、所有角色和制品、完整状态矩阵、全部校验规则、边缘失败、源码路径、证据清单和大规模追问题库。默认只保留 3-5 个最能解释主线的深挖点，除非用户明确要求完整题库。

证据薄弱时，不要强行生成第一人称故事。改为提供项目理解稿、明确未知项和最多三个单一决策问题，不能用通用 Agent 知识虚构个人动作、失败经历或技术取舍。

## 最终检查

回答或保存文件前确认：

- 输出严格符合用户要求的范围和数量；
- 开头存在一句能够独立复述的故事主张；
- 正文只有一条从问题、个人任务、关键设计、完整流程、失败取舍到结果边界的因果主线；
- 每个主表述都有证据，或者明确标记为用户提供；
- 没有混淆项目能力和个人贡献；
- 没有在个人证据缺失时自动使用 `参与`、`负责`、`设计并实现` 等归属动词；
- 学生或实习生亮点收敛到可信的模块、链路、实验、接入或验证职责，没有用“参与”掩盖平台级所有权；
- 入选主题相互独立，并且足以支撑面试；
- 没有虚构指标、规模、生产状态和业务影响；
- 主线只突出不超过三个设计锚点，其他细节已经移入追问附录；
- 同一事实没有在流程、原理、话术和 STAR 中用不同结构反复介绍；
- 30 秒、2 分钟、10 分钟和 STAR 只在用户需要时生成，并且沿用相同关键词、事实和顺序；
- 10 分钟版本是一段可连续讲述的回答，不只是章节目录或时间安排表；
- 没有把正常路径扩大成崩溃恢复，把同进程扩大成分布式恢复，或把内核能力扩大成所有协议均已接入；
- 没有把建议写成强制执行，把候选制品写成已验证或已发布结果，或把关联编号写成严格一次保证；
- 初学者文档遵循“问题、直觉、完整流程、机制、失败与取舍、面试表达”的顺序；
- 必要术语第一次出现时已解释，标题没有堆叠未解释英文标签，源码标识主要位于证据部分；
- 术语定义没有引入第二个未解释缩写或架构标签；
- 图示使用适合读者的标签，并在图后用自然语言走一遍；
- 没有把模拟模型的运行时测试写成真实模型质量评测；
- 证据不足时最多询问三个高价值问题；
- 用户脱离文档后能够复述问题、个人职责、完整流程、一个失败、一个取舍和结果边界；
- 保存的文档包不存在 `TODO`、`TBD`、过期重复主题或失效证据路径。
